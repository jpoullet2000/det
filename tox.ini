[tox]
envlist = py35

[testenv]
deps = -r{toxinidir}/requirements.txt
       -r{toxinidir}/test-requirements.txt
setenv=
     atlas_login = atlas_login
     atlas_password = atlas_password
install_command = 
     pip install --upgrade --allow-all-external {packages}
commands=
     pytest \
      []
